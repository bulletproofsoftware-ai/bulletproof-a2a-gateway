# Runtime dependencies. Pinned to a compatible-release range so patch and
# minor updates flow in while a major bump stays an explicit decision.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
httpx>=0.27,<1.0
pyyaml>=6.0,<7.0
